/**
 * Report Flow Layout
 * Stack layout for the report submission flow.
 */

import { Stack } from 'expo-router';

export default function ReportLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
